# German Coach V4

Upload all files and folders to the root of a GitHub repo and enable GitHub Pages.


## Refactor note
This repo now uses `src/main.js` as the entry point and keeps the legacy application logic in `src/legacyApp.js`. New shared helpers live in `src/core/`.
